---
apiVersion: v1
kind: Service
metadata:
  name: usearch-indexer
  namespace: backend-services
  labels:
    app: usearch-indexer
    code-owner: runwhen
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: usearch-indexer
