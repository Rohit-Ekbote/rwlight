apiVersion: v1
kind: Service
metadata:
  labels:
    app: webhooks
  name: webhooks
  namespace: backend-services
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: webhooks
