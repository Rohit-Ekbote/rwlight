apiVersion: v1
kind: ConfigMap
metadata:
  name: embedder-configmap
  labels:
    app: embedder
  namespace: backend-services
data:
  index.yaml: |
    process:
      TASK_BUFFER_SIZE: 100
      TASK_REQUEST_BATCH_SIZE: 25
