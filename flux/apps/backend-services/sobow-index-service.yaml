apiVersion: v1
kind: Service
metadata:
  name: sobow-index
  namespace: backend-services
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: sobow-index
  type: ClusterIP
