apiVersion: monitoring.googleapis.com/v1alpha1
kind: PodMonitoring
metadata:
  name: corestate-podmonitor
  namespace: corestate
spec:
  selector:
    matchLabels:
      app: corestate
  endpoints:
  - port: 8080
    interval: 30s