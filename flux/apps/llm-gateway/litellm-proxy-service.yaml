apiVersion: v1
kind: Service
metadata:
  name: litellm-proxy
  namespace: llm-gateway
  labels:
    app: litellm-proxy
    code-owner: runwhen
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 4000
    targetPort: 4000
    protocol: TCP
  selector:
    app: litellm-proxy 