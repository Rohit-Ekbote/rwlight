apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-fetching-source
  namespace: flux-system
spec:
  ingress:
  - from:
    - namespaceSelector: {}
    ports:
    - port: 9090
      protocol: TCP
  podSelector: {}
  policyTypes:
  - Ingress
